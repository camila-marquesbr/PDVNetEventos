<Window x:Class="PDVNetEventos.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="220" Width="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Usuário"/>
            <TextBox x:Name="TxtUser" Margin="0,4,0,8"/>

            <TextBlock Text="Senha"/>
            <PasswordBox x:Name="TxtPass" Margin="0,4,0,0"/>
            <TextBlock x:Name="LblStatus" Foreground="Firebrick" Margin="0,8,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Entrar" Width="100" Height="32" Margin="0,8,8,0" Click="Entrar_Click"/>
            <Button Content="Sair"   Width="80" Height="32" Margin="0,8,0,0" Click="Sair_Click"/>
        </StackPanel>
    </Grid>
</Window>